import(methods)
import(RSQLite)

#import(IRanges)

### -----------------------------------------------------------------
### Import S4 classes defined in other packages
###
importClassesFrom(IRanges, SimpleList)
#importClassesFrom(JASPAR2014, JASPAR2014)
importClassesFrom(Biostrings, DNAStringSet, PairwiseAlignments)
importClassesFrom(GenomicRanges, GRanges)

### -----------------------------------------------------------------
### Import S4 methods defined in other packages
###
importMethodsFrom(Biostrings, reverseComplement, matchPWM, maxScore, minScore, consensusMatrix, PairwiseAlignments, pattern)
importMethodsFrom(RSQLite, dbGetQuery, dbConnect, dbDisconnect)
importMethodsFrom(XVector, subseq)
importMethodsFrom(GenomicRanges, seqnames)

### -----------------------------------------------------------------
### Import ordinary functions, variables in other packages
importFrom(seqLogo, seqLogo, makePWM)
importFrom(Biostrings, unitScale, PWMscoreStartingAt, DNAStringSet, DNA_BASES, DNA_ALPHABET, readDNAStringSet, readAAStringSet, writeXStringSet)
importFrom(caTools, runmean)
importFrom(GenomicRanges, GRanges, GRangesList)

### -----------------------------------------------------------------
### Export S4 classes defined in TFBSTools
###
exportClasses(PFMatrix, PWMatrix, ICMatrix,
  PFMatrixList, PWMatrixList, ICMatrixList,
  Site, SiteList
  #PairwiseAlignmentTFBS
  )

### -----------------------------------------------------------------
### Export non-generic functions
###
export(
  ## AllClasses.r
  PFMatrix,
  PFMatrixList,
  #PairwiseAlignmentTFBS,
  ICMatrixList,
  PWMatrixList,
  Site, SiteList
)

### -----------------------------------------------------------------
### Export S4 methods for generics not defined in CNEr
###
exportMethods(
  ## XMatrix-methods.r
  length,
  reverseComplement
)

### -----------------------------------------------------------------
### Export S4 generics defined in TFBSTools + export corresponding methods
###
export(
  ## DB-methods.r
  storeMatrix,
  getMatrixSet,
  getMatrixByID, getMatrixByName,
  deleteMatrixHavingID,
  initializeJASPARDB,

  ## AllGenerics.r
  ID, 
  name, 
  matrixClass, 
  Matrix,
  bg, 
  tags,
  matrixType,
  XMatrixList,
  #alignments,

  ## PWM-methods.r
  toPWM,
  searchSeq,
  searchAln,

  ## ICM-methods.r
  toICM,
  seqLogo,
  totalIC,

  ## Site-methods.r
  writeGFF3, writeGFF2,
  relScore,
  views,

  ## PairwiseAlignment-methods.r
  calConservation,
  #doSiteSearch

  ## Wrappers-methods.r
  runMEME,
  sitesSeq
)


